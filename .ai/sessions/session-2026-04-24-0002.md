# Session Handoff — kor-legal-mcp — 2026-04-24-0002

## 0. 세션 성격

GitHub → GitLab 동기화 후 `enforcement_date` propagate fix(commit `3c606a4`) 포털 배포 완료 확인 세션.

---

## 1. 입력 컨텍스트

- 세션 시작 시 main: `887b30d` (GitHub 기준)
- 이전 세션(`0001`)에서 `enforcement_date` / `last_amended` P0 fix 코드 작성 완료, GitHub push 대기 상태였음

---

## 2. 본 세션에서 한 일

### 2.1 GitHub → GitLab 동기화

```
git fetch github main
git merge github/main   # fast-forward, 887b30d → 5432a85 (3 commits)
git push origin main    # GitLab 반영
```

반영된 커밋:

| 커밋 | 메시지 |
|------|--------|
| `3c606a4` | fix(tools): propagate enforcement_date / last_amended through search_law and get_law_article |
| `3aeee8a` | docs(.ai): portal instructions for enforcement_date propagation fix |
| `5432a85` | chore: add session 2026-04-24-0001 handoff |

### 2.2 포털 배포 완료 (사용자 직접 수행)

포털 Web IDE에서 `git pull` 후 수동 배포 트리거 완료.

### 2.3 배포 검증 (smoke test)

엔드포인트: `https://portal-serving-evangelist-1-mcp-c94cc9c5.samsungsdscoe.com`

| 항목 | 결과 |
|------|------|
| `GET /healthz` | ✅ `{"status":"ok","version":"1.0.0","components":{"law_api":"ok"}}` |
| `initialize` serverInfo.name | ✅ `"kor-legal-mcp"` |
| `search_law` → `enforcement_date` | ✅ `"20260303"`, `last_amended: "20251202"` |
| `get_law_article` → `enforcement_date` | ✅ `"20260303"`, `last_amended: "20251202"` |

비고: `get_law_article` 첫 호출 시 law.go.kr ReadError 1회 발생 → 재시도 성공. 서버 이상 없음.

---

## 3. 현재 상태

- ✅ GitHub `youngs75/kor-legal-mcp` HEAD: `5432a85`
- ✅ 포털 GitLab HEAD: `5432a85`
- ✅ 포털 배포 완료 (`enforcement_date` fix 반영)
- ✅ `search_law` / `get_law_article` 응답에 `enforcement_date` / `last_amended` 실제 값 포함 확인
- ✅ `apt-legal-agent` sufficiency loop 시행일자 결정론 검증 활성화 가능 상태

---

## 4. 다음 단계

### 🔵 단기 — apt-legal-agent 연동
- `apt-legal-agent` sufficiency loop 배포 시 `Citation.effective_date` 검증 게이트 자동 활성화됨
- 배포 전엔 게이트가 항상 `None`으로 무력화 상태였음

### 🟢 중기 — Track B P1/P2
- `get_law_article` 응답에 `law_id` (MST, 법령일련번호) 추가
- `amendment_history` 채우기 (현재 모두 `null`)
- 단위 테스트 확충 — 15개 tool handler 커버리지 0%

### 🔴 장기
- 캐시 키에 schema version 포함 (구 캐시 응답에 새 필드 없는 문제)
- PostgreSQL L2 캐시 설계
- Milvus 임베딩 검색 PoC

---

## 5. 중요 참고사항

### 리포 동기화 구조
- **GitHub**: `youngs75/kor-legal-mcp` (remote명 `github`)
- **포털 GitLab**: `gitlab.samsungsdscoe.com/088dac94-.../apt-legal-mcp.git` (remote명 `origin`)
- **동기화 방향**: GitHub → `git fetch github main` → `git merge` → `git push origin main`

### 운영 엔드포인트
- **kor-legal-mcp**: `https://portal-serving-evangelist-1-mcp-c94cc9c5.samsungsdscoe.com`
- **healthz**: `/healthz`, **MCP**: `/mcp` (suffix 필수)

### 코드/빌드 함정 (누적)
- `field_validator(...)(lambda)` 금지 — `Annotated + BeforeValidator` 사용
- `FastMCP`의 `streamable_http_app()`은 자체 `/mcp` 라우트 보유 → `Mount("/", ...)` 사용
- `TransportSecuritySettings(enable_dns_rebinding_protection=False)` 필수 (프록시 뒤)
- **MCP tool 응답은 원문 그대로** — `truncate()` 금지
- **ordin detail은 `ID=자치법규ID`**
- **CustomGPT MCP connector URL은 반드시 `.../mcp` suffix 포함**
- **법령명 매칭**: exact match → 가장 짧은 substring → 첫 번째 hit 순서

### 환경 / 인프라
- **로컬**: Citrix VDI Windows 11, PowerShell, Python 3.14
- **Docker 불가**: WSL2 미지원. 빌드/배포는 포털 Web IDE
- **프록시**: `HTTPS_PROXY=http://70.10.15.10:8080`, MITM CA
- **law.go.kr OC**: `apt-legal-agent`
- **Web IDE에서 uv 없음** — `pip install -e ".[dev]"` 사용
