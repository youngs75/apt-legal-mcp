# Session 2026-04-13-0002

## 프로젝트 개요
`apt-legal-mcp` — 한국 공동주택 법률 자문 Vertical AI Agent(A2A 지원)가 호출하는 MCP 서버.
국가법령정보센터 Open API를 1차 데이터 소스로 사용. 파이프라인: ChatGPT Enterprise CustomGPT → apt-legal-agent(별도 리포) → 본 MCP 서버 → law.go.kr.

## 최근 작업 내역 (0001 이후)
1. **Dockerfile 작성 (포털 배포 전제조건 충족)**
   - 초기에 `python:3.12-slim` 기반으로 작성 후, 사용자 확인으로 **`ghcr.io/astral-sh/uv:python3.12-bookworm-slim`** 베이스로 교체
   - `uv sync --frozen --no-dev`로 `uv.lock` 기반 재현성 확보, 의존성 레이어와 소스 레이어 분리
   - `libxml2`/`libxslt1.1` 시스템 의존성 설치, 비루트 유저(`app` uid 1000), `/healthz` HEALTHCHECK, 포트 8001
2. **패키지 매니저 `uv` 전환**
   - Web IDE에서 `uv sync --extra dev` 실행 성공 → `uv.lock` 생성/커밋
   - pip 대비 2~5배 빠른 빌드, lockfile 기반 재현성
3. **교육 포털(Samsung SDS COE) 환경 파악**
   - AWS EKS 기반, Web IDE(VS Code, Python 3.12 전용 이미지), MCP/Agent 배포, Milvus(RAG), PostgreSQL 지원
   - 단일 공유 계정 구조 → 개인 GitLab PAT 발급 불가
   - 포털 GitLab: `https://gitlab.samsungsdscoe.com/088dac94-f2c1-49f7-bfbf-828205f860d4/apt-legal-mcp.git`
   - Web IDE 컨테이너: `root@ws-...`, bash, 외부 네트워크 OK
   - 포털 규격: **프로젝트 루트에 `Dockerfile` 필수**
4. **삼각 동기화 워크플로우 수립 (로컬 / GitHub / 포털 GitLab)**
   - 문제: 포털 GitLab은 Web IDE 세션 인증만 사용 가능, 로컬 VDI에서 직접 push 불가(인증 정보 없음)
   - 해결: **로컬 VDI → GitHub (주 저장소) → Web IDE 터미널에서 github/main fetch → 포털 GitLab으로 push**
   - Web IDE 터미널에서 github remote 추가, `.devcontainer/devcontainer.json`(포털 기본 제공) 보존 및 커밋
5. **세 리포 동기화 완료**
   - 로컬 `C:\projects\aptlaw-mcp` / GitHub `youngs75/apt-legal-mcp` / 포털 GitLab 모두 동일 커밋 `613a487`
   - 파일: `Dockerfile`, `.devcontainer/devcontainer.json`, `uv.lock` 포함 전체 스캐폴드

## 현재 상태
- ✅ 코드·의존성·Dockerfile·devcontainer·uv.lock 전부 커밋 및 세 리포 동기화 완료
- ✅ 포털 배포 전제조건(루트 Dockerfile) 충족
- ✅ 개발 환경 두 곳(로컬 VDI Python 3.14 / Web IDE Python 3.12) 모두 동작 확인
- ⏳ **API 키 대기 중** (`LAW_API_KEY`, 국가법령정보센터 Open API) — 발급에 시간 걸림
- ⚠️ 포털에서 실제 배포 버튼/절차는 아직 미실행 (API 키 전에 시도해볼 수는 있음)
- ⚠️ `search_interpretation` 스텁 상태 (법령해석례 API 연동 필요)
- ⚠️ `law_api.py`의 판례 XML 필드명 미검증 (API 키 후 PoC로 보정)
- ⚠️ Milvus/PostgreSQL 활용 전략 아직 설계 전

## 다음 단계
1. **포털 배포 smoke test** (API 키 없어도 가능)
   - 포털에서 MCP 배포 버튼 → Dockerfile 빌드/실행 확인
   - `/healthz`, `/` 엔드포인트 응답 확인 (API 키 없으면 law_api ping만 unavailable 표시될 것)
   - MCP Streamable HTTP `/mcp` 엔드포인트 연결 확인
2. **API 키 수령 후 PoC** (우선순위 순, 0001 세션에 상세 기록)
   - `search_laws`/`get_law_detail` 실제 응답 필드명 검증
   - `search_precedents`/`get_precedent_detail` 필드명 보정
   - 법령해석례 API → `search_interpretation` 실구현 교체
   - 중앙환경분쟁조정위원회 데이터 통합 (층간소음 품질 향상)
   - 지능형 법령검색 API 품질 비교
3. **Milvus 통합 설계** (PoC 이후)
   - 법령 조문 임베딩 → 의미 기반 `search_law`
   - 판례 유사 사례 검색 RAG
   - 임베딩 모델 결정 필요 (포털 제공 여부 확인, 없으면 OpenAI text-embedding-3-small)
4. **PostgreSQL L2 캐시 설계** (선택)
   - 현 `MemoryCache`를 L1으로 유지, PG를 L2 영속 캐시로 추가
   - Pod 재시작 시에도 law.go.kr 응답 재활용
5. **Tool 단위 테스트 추가** (`pytest-httpx`로 law.go.kr mock)
6. **Vertical Agent 리포(`apt-legal-agent`) AGENTS.md 개편** — 현재 MCP 서버용 내용이 복사된 상태

## 중요 참고사항
- **개발 환경 이원화**
  - **로컬 VDI**: Windows 11 + Citrix + PowerShell, Python 3.14, 주 개발 위치, GitHub으로만 push
  - **Web IDE**: 포털 제공 Linux 컨테이너(bash, root), Python 3.12 고정, GitLab 세션 인증, github remote 수동 추가됨
- **삼각 동기화 명령어**
  - 로컬에서: `git push origin main` (→ GitHub)
  - Web IDE에서: `git fetch github && git reset --hard github/main && git push origin main` (→ GitLab)
- **포털 정보**
  - URL: `https://gitlab.samsungsdscoe.com/088dac94-f2c1-49f7-bfbf-828205f860d4/`
  - Web IDE 이미지: `755035179509.dkr.ecr.us-east-1.amazonaws.com/mspsa/agent-portal/agent-portal-vscode-python312:latest`
  - 외부 네트워크 제약 없음 (ghcr.io, law.go.kr, GitHub 등 모두 접근 가능)
  - 지원 서비스: MCP 배포 / Agent 배포 / Milvus(RAG) / PostgreSQL
  - 공유 계정 구조 → 개인 PAT 발급 불가, Web IDE 세션 인증만 가능
- **API 키 주의**
  - `LAW_API_KEY`는 일반 토큰이 아닌 **가입 이메일 앞부분**(`OC` 파라미터). 예: `youngs75@example.com` → `LAW_API_KEY=youngs75`
  - 발급까지 영업일 1~2일 소요 예상
- **배포 시 시크릿 주입**: `.env`는 로컬 개발용, 배포 시 포털 환경변수 기능 또는 k8s Secret 사용 예정
- **Dockerfile 베이스**: `ghcr.io/astral-sh/uv:python3.12-bookworm-slim` — 포털이 ghcr.io 접근 가능 확인됨
- **판례 XML 필드명 추정**: `판례일련번호`, `사건번호`, `사건명`, `법원명`, `선고일자`, `사건종류명`, `판시사항`, `판결요지`, `판례내용`, `주문`, `참조조문` — 실제 응답과 다를 수 있음, PoC에서 보정 필요
- **리포 URL 정리**
  - GitHub (public): https://github.com/youngs75/apt-legal-mcp
  - 포털 GitLab: https://gitlab.samsungsdscoe.com/088dac94-f2c1-49f7-bfbf-828205f860d4/apt-legal-mcp.git
- **현재 커밋**: `613a487 build: switch Dockerfile to uv-based build`
