# Session 2026-04-16-0001

## 프로젝트 개요
`kor-legal-mcp` — 한국 법령·판례·행정해석·헌재결정·행정규칙·자치법규·조약 조회 **범용** MCP 서버.
국가법령정보센터(law.go.kr) Open API를 1차 데이터 소스로 사용하며, 현재 15개 tool을 제공한다.
운영 파이프라인은 **ChatGPT Enterprise CustomGPT `한국 법령 리서처` → kor-legal-mcp → law.go.kr**로, Vertical AI Agent 경유 없이 CustomGPT가 MCP를 직접 호출하는 구성이다.

## 최근 작업 내역 (0001 이후)

### 1. GitHub remote URL 갱신
- 리포 이름이 `apt-legal-mcp` → `kor-legal-mcp`로 변경됨.
- 로컬의 `github` remote URL을 `https://github.com/youngs75/kor-legal-mcp.git`으로 업데이트.
  ```
  git remote set-url github https://github.com/youngs75/kor-legal-mcp.git
  ```

### 2. GitHub 최신 커밋 반영 (2282c58)
`git pull github main`으로 GitHub의 최신 4개 커밋을 로컬에 적용:

| 커밋 | 내용 |
|------|------|
| `2282c58` | fix(tools): prefer exact law name match in get_article to avoid 시행령/시행규칙 confusion |
| `314edd3` | docs(AGENTS): add Memory Sync section |
| `0b661d8` | docs: refresh banners + session-2026-04-15-0001 handoff |
| `420820d` | docs: session-2026-04-14-0003 handoff |

### 3. 포털 배포 작업지시서 실행
Web IDE에서 아래 단계를 순차 수행:

| 단계 | 결과 |
|------|------|
| Step 1: GitHub 2282c58 확인 | ✅ 이미 적용 완료 |
| Step 2: github/main overlay (`git checkout github/main -- .`) | ✅ 변경사항 없음 (HEAD 이미 동일) |
| Step 3: GitLab push (`351c83b → 2282c58`, 4 commits) | ✅ 성공 |
| Step 4: 의존성 설치 (`pip install -e ".[dev]"`) | ✅ kor-legal-mcp 1.0.0 + 전체 의존성 설치 완료 |
| Step 5: `pytest tests/test_law_match.py -v` | ✅ 5/5 PASSED |
| Step 6: 포털 UI 수동 배포 트리거 | → 사용자 직접 수행 |
| Step 7: 헬스체크 | → 배포 후 확인 필요 |

### 4. 핵심 버그픽스 내용 (2282c58)
`get_law_article` tool에서 법령 검색 시 시행령·시행규칙이 우선 매칭되는 문제를 수정.
- **변경 파일**: `src/kor_legal_mcp/clients/law_api.py`
- **로직**: 검색 결과 hits 중 exact match → 가장 짧은 substring match → 첫 번째 hit 순서로 우선순위 부여
- **검증**: `tests/test_law_match.py` 5개 테스트 (exact/substring/fallback/empty) 모두 통과

## 현재 상태
- ✅ GitHub `youngs75/kor-legal-mcp` HEAD: `2282c58`
- ✅ 포털 GitLab HEAD: `2282c58` (push 완료)
- ✅ 15개 tool production에서 동작 확인 (CustomGPT에서 실전 검증, 이전 세션)
- ✅ `test_law_match.py` 5/5 PASSED
- ⏳ 포털 재배포: 사용자 직접 수행 필요 (Web IDE → 수동 배포 트리거)
- ⏳ 재배포 후 헬스체크: `GET /healthz` → `{"status": "ok"}` 확인 필요
- ⚠️ ANTHROPIC_API_KEY rotate 미처리 (low priority, 이월)

## 다음 단계

### 즉시 (본 세션 이후)
1. **포털 UI 수동 배포 트리거** — 사용자가 직접 Web IDE에서 수행
2. **헬스체크 확인** — 배포 완료 후:
   ```bash
   curl -s https://portal-serving-evangelist-1-mcp2-c01386b6.samsungsdscoe.com/healthz | python -m json.tool
   ```

### 우선순위 중간 (이전 세션에서 이월)
3. **`apt-domain-mcp` 신규 리포 설계** — 특정 단지 운영규약 + 지자체 조례 RAG. apt-domain-mcp 스코프는 단지/관리규약 RAG에 집중.
4. **단위 테스트 확충** — 15개 tool handler + schema 커버리지 0%. `pytest-httpx`로 law.go.kr 응답 mock.
5. **`apt-legal-agent` Vertical Agent 리포 정리** — Phase 2 대비.

### 우선순위 낮음
6. **PostgreSQL L2 캐시 설계**
7. **Milvus 임베딩 검색 PoC**
8. **`detc` 빈 응답 케이스 처리**
9. **`docs/01~03_*.md` 본문 본격 재작성**

## 중요 참고사항

### 운영 파이프라인 (현 시점 확정)
```
ChatGPT Enterprise CustomGPT (한국 법령 리서처)
       ↓ Streamable HTTP (.../mcp suffix 필수)
kor-legal-mcp (AWS EKS, Samsung SDS CoE 포털)
       ↓ REST
law.go.kr Open API
```
Vertical Agent 경유는 Phase 2 옵션.

### 리포 동기화 구조
- **GitHub**: `youngs75/kor-legal-mcp` (주 개발 리포, remote명 `github`)
- **포털 GitLab**: `gitlab.samsungsdscoe.com/088dac94-.../apt-legal-mcp.git` (remote명 `origin`, 배포 소스)
- **동기화 방향**: GitHub → 로컬 pull → GitLab push
- **주의**: remote명은 `github` / `origin`, GitLab URL의 리포명은 여전히 `apt-legal-mcp.git`이지만 실제 코드는 `kor-legal-mcp` 기준

### 코드/빌드 함정 (누적)
- `field_validator(...)(lambda)` 금지 — `Annotated + BeforeValidator` 사용
- `FastMCP`의 `streamable_http_app()`은 자체 `/mcp` 라우트 보유 → `Mount("/", ...)` 사용
- `TransportSecuritySettings(enable_dns_rebinding_protection=False)` 필수 (프록시 뒤)
- **MCP tool 응답은 원문 그대로** — `truncate()` 금지
- **ordin detail은 `ID=자치법규ID`** — 검색 결과의 `자치법규일련번호`나 MST가 아님
- **CustomGPT MCP connector URL은 반드시 `.../mcp` suffix 포함**
- **법령명 매칭**: exact match → 가장 짧은 substring → 첫 번째 hit 순서 (2282c58 이후)

### 환경 / 인프라
- **로컬**: Citrix VDI Windows 11, PowerShell, Python 3.14
- **Docker 불가**: WSL2 미지원. 빌드/배포는 포털 Web IDE
- **프록시**: `HTTPS_PROXY=http://70.10.15.10:8080`, MITM CA
- **law.go.kr OC**: `apt-legal-agent`
- **Web IDE에서 uv 없음** — `pip install -e ".[dev]"` 사용

### 검증 워크플로우
1. `pytest tests/test_law_match.py -v` — 법령명 매칭 로직 (5개)
2. `scripts/verify_tools.py` — 18개 tool 회귀
3. `scripts/verify_via_claude.py` — 배포된 MCP에 Claude API 직접 붙어 실전 검증 (재배포 후 권장)
